// import {IPrivateUserData} from './types'

// declare global {
//   interface UserPrivateMetadata extends IPrivateUserData {
//     [k: string]: unknown; // i add this just because otherwise i get build error but it does not help
//   }
// }
